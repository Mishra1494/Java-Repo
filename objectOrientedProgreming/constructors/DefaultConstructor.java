class DefaultConstructor
{
       int num;
       public static void main(String[] args)
       {
            DefaultConstructor SampleObj = new DefaultConstructor();
            //new is used for creation of object
            //here user is not definig any constructor so the the complier will create  it 
            //but no argument will pass 
       }
}